import React from 'react';
import Menu from './Menu';

const Portfolio: React.FC = () => {

    return (
        <div>
            <h1>Hello, World!</h1>
            <Menu />
        </div>
    );
};

export default Portfolio;